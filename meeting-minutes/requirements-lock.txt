# 已实测通过的版本组合（2026-09，Python 3.9.7 / Windows）
# 优先用它，能保证不同电脑上装出来是同一套东西。
# 注意：如果本机 Python 是 3.13 等很新的版本，个别包可能没有对应 wheel，
#       这时改用 requirements.txt（宽松版）。
av==15.1.0
faster-whisper==1.2.1
ctranslate2==4.8.2
tokenizers==0.22.2
huggingface-hub==1.8.0
onnxruntime==1.19.2
numpy==2.0.2
sherpa-onnx==1.13.8
python-docx==1.2.0
python-pptx==1.0.2
lxml==6.1.3
Pillow==11.3.0
